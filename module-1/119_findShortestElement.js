/*
function getCount(str) {
  return (str.match(/[aeiou]/gi) || []).length;
}

var output = getCount("my pyx");
console.log(output); // --> 'a'
*/

function persistence(num) {
  numArr = num.toString();
}

let num = 999;
console.log(persistence(num));
